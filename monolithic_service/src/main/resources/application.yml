server:
  port: 8084
  http2:
    enabled: true

spring:
  application:
    name: monoliticService
  jpa:
    open-in-view: true
    hibernate:
      ddl-auto: none
  resources:
    add-mappings: true